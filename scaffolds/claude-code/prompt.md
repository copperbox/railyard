A `{{type}}` signal arrived from {{source.kind}} `{{source.name}}`.

Payload:

```json
{{payload}}
```

Describe, in one short paragraph, what this payload appears to represent.
Do not use any tools; just answer.
